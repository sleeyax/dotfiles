# Packages installed by scripts/apply.sh, via yay or paru.
# One package per line; '#' starts a comment.
# Grouped by subsystem so dropping a subsystem is deleting a labelled block.
#
# This is what every machine gets, headless ones included.
# Anything that wants a display belongs in packages.graphical.txt instead.

# --- Base tooling ---
git
curl
wget
rsync
stow
tar
unzip
jq
inotify-tools
pacman-contrib
libsecret

# --- Editors ---
vim
neovim

# --- Shell and prompt ---
zsh
oh-my-posh-bin
fzf
eza
fastfetch
htop
btop

# --- Python and build ---
python-pip
cargo
